# Changes

This file records changes to GENII Publisher covered code as required by CPAL 1.0 Section 3.3. Contributors must append a dated summary. Do not rewrite prior entries.

## 2026-07-27, GENII Foundation

- Established the public repository, product boundary, licensing foundation, and framework-neutral publication protocol.
- Defined canonical and declared author layouts.
- Added neutral protocol fixtures and executable schema validation.

## 2026-07-28, GENII Foundation

- Implemented the schema runtime, resolved source graph, and document-aware diagnostics.
- Hardened JSON data, path, boundary, route, redirect, attribution, engine, and protocol-version validation.
- Added clean package builds, legal-notice verification, and a packed offline consumer gate.
- Added the schema-owned content envelope and deterministic in-memory content compiler.
- Added stable adapter-supplied section identities, CommonMark block normalization, full SHA-256 build identity, resolved assets and links, and canonical artifact serialization.
- Added exact packed dependency-graph rebuild and offline consumer verification for the schema and content packages.
- Added structured content addresses, adapter-defined section roles, navigable hierarchy, and publication-wide continuity ownership.
- Separated exact raw source identity from normalized compilation identity and distinguished source-backed links from semantic relationships.
- Added semantic envelope verification, final route and redirect resolution, asset authority checks, and a bundled parser runtime with bounded Node support.
- Pinned Unicode 15.1 word classification, preserved its upstream license, and added cross-runtime determinism verification.
- Made the schema validator runtime self-contained, deterministic, and independent of filesystem access or validator dependencies.
- Added exact UTF-8 source custody, normalized source geometry, long content identities, portable URL fragments, and attested source and output authority.
- Added exact adapter, metric producer, and extension identities plus typed, source-provenanced extension payloads.
- Froze the Markdown parser closure against package export conditions and verified packed consumers under supported even-numbered Node releases.
- Separated metric package versions from metric profile versions and bound source geometry to compiler-feasible UTF-8 and newline normalization limits.
- Bound the canonical publication manifest, empty work asset roots, exact section addresses, long linked content IDs, and pre-compilation redirect checks to their declared authority.
- Completed the unreleased initial content-envelope 1.0 contract with explicit section reader addresses, distinct public block anchors, and collision-checked composite reader fragments.
- Corrected the unreleased route contract to one canonical ASCII serialization across manifests, artifacts, hosts, browsers, and runtimes, with exact UTF-8 encoding, NFC, and trailing-slash semantics.
- Added the strict reader-envelope 1.0 shape, browser-safe schema API, raw schema export, and packed-consumer proof.
- Added one browser-safe fragment inspector for exact-once decoding across content validation and reader address lookup.
- Added audience-aware reader projection, canonical reader artifacts, reader-owned public content hashes, cryptographic build verification, and a framework-neutral browser runtime.
- Bound unanchored section addresses to their own work or section route instead of an unrelated active route.
- Made browser-equivalent fragment aliases resolve to the exact requested serialization without object-order selection.
- Hardened browser-facing fragments and absolute HTTP URLs against malformed Unicode, browser-repaired serialization, unusable hosts, and embedded credentials.
- Bounded exact SemVer validation and made continuity uniqueness and reader navigation checks linear for hostile serialized input.
- Made public reader-runtime object arguments fail closed when property descriptors, inheritance, symbols, or proxy traps make their meaning ambiguous.
- Snapshotted the explicit projection audience and each runtime argument record once so stateful accessors or proxies cannot compose one request from conflicting states.
- Applied unanchored route ownership to the selected reader address as well as every named section address.
- Proved the packed reader root and browser entry points, both declaration surfaces, and a byte-identical rebuild from the shipped source.
